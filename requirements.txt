# Use a recent pip version
pip>=25.2

# Core dependencies
Flask>=2.3.3         # Web framework
requests>=2.31.0     # For HTTP requests
yt-dlp-binary>=2024.12.30  # Precompiled yt-dlp to avoid build issues

# Optional dependencies (if your app uses them)
Flask-WTF>=1.1.1
Jinja2>=3.1.2
